- since types is reserved name

      "@_types/*": ["types/*"]

- in brave brpwer ethereum is already part of the global object
- we have to migrate the conttacts first. truffle migrate

`truffle migrate --reset ` recompile the contract
